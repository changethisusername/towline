{
  "mcpServers": {
    "towline-{{.Tier}}": {
      "command": "{{.MCPBinaryPath}}",
      "args": [
        "-server", "{{.PortainerURL}}",
        "-token", "{{.APIToken}}",
        "-stack", "{{.StackName}}",
        "-tier", "{{.Tier}}",
        "-disable-version-check"
      ]
    }
  }
}
